<?php
// These are categories for the vip shop. Pay close attention to the numbers,
// as these numbers are stored in the database when you add an service to a specific
// category, so it knows which category the service belongs to.
// You may add/remove categories from here as you see fit.
// Category name / item image (id)
return array(
	0 => array('Cash points', 1000393),		// #CASHPOINTS
	1 => array('Kafra points', 1000392),	// #KAFRAPOINTS
	2 => array('VIP time', 1000103),	// VIP time
);
?>
